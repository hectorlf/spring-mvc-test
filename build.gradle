subprojects {
    apply plugin: 'java'
    repositories {
        flatDir { dirs 'lib' }
        mavenCentral()
    }
}

project(':spring-mvc-test') {
    dependencies {
        compile project(path: ':spring-mvc-test-model', configuration: 'woven')
    }
}