subprojects {
    apply plugin: 'java'
    repositories {
        flatDir { dirs 'lib' }
        mavenCentral()
    }
}


project(':spring-mvc-test') {
    dependencies {
        compile project(':spring-mvc-test-model')
    }
}